# LangGraph Studio Requirements - Complete Setup
# Core LangGraph dependencies with CLI and API support
langgraph>=0.5.0
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-openai>=0.2.0,<0.3.0
langchain-community>=0.3.0,<0.4.0
langsmith>=0.4.0

# LangGraph CLI with development server support
langgraph-cli[inmem]>=0.3.4

# Essential runtime dependencies
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.4.0,<3.0.0
openai>=1.55.3,<2.0.0
tiktoken>=0.5.2,<1.0.0

# HTTP client - pinned to avoid conflicts
httpx>=0.25.0,<0.28.0
requests>=2.31.0,<3.0.0

# Database connectivity for the agent
supabase>=2.3.4,<3.0.0
asyncpg>=0.29.0,<1.0.0
sqlalchemy>=2.0.23,<3.0.0

# Utilities
python-dotenv>=1.0.0,<2.0.0
tenacity>=8.2.3,<9.0.0

# Note: watchfiles is installed as dependency of langgraph-api
# This is required for LangGraph Studio development server
# The newer version (1.1.0+) should resolve previous conflicts

# Development tools (minimal set)
structlog>=23.2.0,<25.0.0

# Document processing (if needed by the agent)
pypdf>=3.17.4,<5.0.0
python-multipart>=0.0.6,<1.0.0 